# web_api/ -- отдельный процесс от бота, не входит в requirements.txt в корне репозитория (тот
# же принцип, что и у scripts/postgres_migration.requirements.txt: свой набор зависимостей для
# своего процесса). Устанавливать из корня репозитория:
#   pip install -r web_api/requirements.txt
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
